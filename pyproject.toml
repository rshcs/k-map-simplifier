[project]
name = "kmap-simplifier"
version = "0.1.0"
description = "Four-variable Karnaugh map simplifier"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[project.scripts]
kmap = "app:main"
